@model List<Medicare_Connect.Areas.Patients.Models.Reminder>
@{
    ViewData["Title"] = "Reminders";
}

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2 class="h4 mb-0">Izikhumbuzi (Reminders)</h2>
        <a class="btn btn-outline-primary" asp-area="Patients" asp-controller="Dashboard" asp-action="Index">Back to dashboard</a>
    </div>
    <p class="text-muted">Create reminders for appointments, medication, and custom notes.</p>

    @if (TempData["RemSuccess"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="bi bi-check-circle me-2"></i>@TempData["RemSuccess"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }
    @if (TempData["RemError"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <i class="bi bi-exclamation-triangle me-2"></i>@TempData["RemError"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <div class="row g-4">
        <div class="col-lg-4">
            <div class="card dashboard-card border-0">
                <div class="card-body">
                    <h5 class="card-title mb-3">Create Reminder</h5>
                    <form asp-action="Create" method="post">
                        @Html.AntiForgeryToken()
                        <div class="mb-3">
                            <label class="form-label">Title</label>
                            <input class="form-control" type="text" name="Title" maxlength="100" required />
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Type</label>
                            <select class="form-select" name="Type" required>
                                <option value="Appointment">Appointment</option>
                                <option value="Medication">Medication</option>
                                <option value="Custom" selected>Custom</option>
                            </select>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Due Date & Time (optional)</label>
                            <input class="form-control" type="datetime-local" name="DueAt" />
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Notes (optional)</label>
                            <textarea class="form-control" name="Notes" rows="2" maxlength="256"></textarea>
                        </div>
                        <div class="d-grid">
                            <button class="btn btn-primary" type="submit">
                                <i class="bi bi-plus-circle me-2"></i>Add Reminder
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
        <div class="col-lg-8">
            <div class="card dashboard-card border-0">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center mb-3">
                        <h5 class="card-title mb-0">Your Reminders</h5>
                    </div>
                    <div class="table-responsive">
                        <table class="table table-hover align-middle">
                            <thead>
                                <tr>
                                    <th>Title</th>
                                    <th>Type</th>
                                    <th>Due</th>
                                    <th>Status</th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody>
                                @if (!Model.Any())
                                {
                                    <tr>
                                        <td colspan="5" class="text-center text-muted">No reminders yet.</td>
                                    </tr>
                                }
                                else
                                {
                                    @foreach (var r in Model)
                                    {
                                        <tr>
                                            <td>@r.Title</td>
                                            <td>@r.Type</td>
                                            <td>@(r.DueAt.HasValue ? r.DueAt.Value.ToString("ddd, dd MMM yyyy HH:mm") : "—")</td>
                                            <td>
                                                @if (r.IsCompleted)
                                                {
                                                    <span class="badge bg-success">Completed</span>
                                                }
                                                else
                                                {
                                                    <span class="badge bg-warning text-dark">Pending</span>
                                                }
                                            </td>
                                            <td class="text-end">
                                                <form asp-action="Toggle" method="post" class="d-inline">
                                                    @Html.AntiForgeryToken()
                                                    <input type="hidden" name="id" value="@r.Id" />
                                                    <button class="btn btn-sm btn-outline-secondary" type="submit">
                                                        <i class="bi bi-check2-square me-1"></i>@(r.IsCompleted ? "Mark Pending" : "Mark Done")
                                                    </button>
                                                </form>
                                                <form asp-action="Delete" method="post" class="d-inline ms-1">
                                                    @Html.AntiForgeryToken()
                                                    <input type="hidden" name="id" value="@r.Id" />
                                                    <button class="btn btn-sm btn-outline-danger" type="submit">
                                                        <i class="bi bi-trash me-1"></i>Delete
                                                    </button>
                                                </form>
                                            </td>
                                        </tr>
                                    }
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>


