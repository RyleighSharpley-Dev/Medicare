@{
    ViewData["Title"] = "Medicare Connect";
    ViewData["BodyClass"] = "landing-page";
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Medicare Connect - Healthcare Made Simple</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary: #2a7de1;
            --primary-dark: #1e5ca8;
            --secondary: #6c757d;
            --success: #28a745;
            --light: #f8f9fa;
            --dark: #343a40;
            --white: #ffffff;
            --gradient: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
            --shadow: 0 5px 15px rgba(0, 0, 0, 0.08);
            --radius: 12px;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
            line-height: 1.6;
            color: var(--dark);
            background-color: var(--white);
            overflow-x: hidden;
        }

        /* Hero Section */
        .hero {
            position: relative;
            min-height: 100vh;
            display: flex;
            align-items: center;
            overflow: hidden;
            /* Background image directly on section */
            background: url("https://images.unsplash.com/photo-1588776814546-ec7d3c11e3a5?ixlib=rb-4.0.3&auto=format&fit=crop&w=1920&q=80") no-repeat center center/cover;
        }


        .hero__image {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            object-fit: cover;
            z-index: 1;
        }

        .hero__overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, rgba(42, 125, 225, 0.85) 0%, rgba(30, 92, 168, 0.75) 100%);
            z-index: 2;
        }

        .hero__content {
            position: relative;
            z-index: 3;
            color: var(--white);
            padding: 2rem 1rem;
            max-width: 1200px;
            margin: 0 auto;
        }

        .hero__eyebrow {
            font-size: 1rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 2px;
            margin-bottom: 1rem;
            opacity: 0.9;
        }

        .hero__title {
            font-size: 3.5rem;
            font-weight: 700;
            line-height: 1.2;
            margin-bottom: 1.5rem;
        }

        .hero__subtitle {
            font-size: 1.25rem;
            max-width: 600px;
            margin-bottom: 2.5rem;
            opacity: 0.9;
        }

        .hero__cta .btn {
            padding: 0.75rem 2rem;
            font-weight: 600;
            border-radius: 50px;
            transition: all 0.3s ease;
            box-shadow: var(--shadow);
        }

        .hero__cta .btn-light {
            background-color: var(--white);
            color: var(--primary);
            border: 2px solid var(--white);
        }

            .hero__cta .btn-light:hover {
                background-color: transparent;
                color: var(--white);
                transform: translateY(-3px);
            }

        .hero__cta .btn-outline-light {
            border: 2px solid var(--white);
            background-color: transparent;
        }

            .hero__cta .btn-outline-light:hover {
                background-color: var(--white);
                color: var(--primary);
                transform: translateY(-3px);
            }

        /* Features Section */
        .features {
            padding: 5rem 1rem;
            background-color: var(--white);
        }

        .section-title {
            text-align: center;
            margin-bottom: 3rem;
        }

            .section-title h2 {
                font-size: 2.5rem;
                font-weight: 700;
                color: var(--dark);
                margin-bottom: 1rem;
            }

            .section-title p {
                font-size: 1.1rem;
                color: var(--secondary);
                max-width: 700px;
                margin: 0 auto;
            }

        .feature-card {
            background: var(--white);
            border-radius: var(--radius);
            padding: 2rem;
            height: 100%;
            transition: all 0.3s ease;
            box-shadow: var(--shadow);
            border: 1px solid rgba(0, 0, 0, 0.05);
        }

            .feature-card:hover {
                transform: translateY(-10px);
                box-shadow: 0 15px 30px rgba(0, 0, 0, 0.1);
            }

        .feature-icon {
            width: 70px;
            height: 70px;
            background: var(--gradient);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 1.5rem;
        }

            .feature-icon i {
                font-size: 1.8rem;
                color: var(--white);
            }

        .feature-card h3 {
            font-size: 1.5rem;
            font-weight: 600;
            margin-bottom: 1rem;
            color: var(--dark);
        }

        .feature-card p {
            color: var(--secondary);
        }

        /* How It Works Section */
        .how-it-works {
            padding: 5rem 1rem;
            background-color: #f8fafc;
        }

        .step-card {
            text-align: center;
            padding: 2rem 1rem;
        }

        .step-number {
            width: 60px;
            height: 60px;
            background: var(--gradient);
            color: var(--white);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            font-weight: 700;
            margin: 0 auto 1.5rem;
        }

        .step-card h3 {
            font-size: 1.4rem;
            font-weight: 600;
            margin-bottom: 1rem;
        }

        .step-card p {
            color: var(--secondary);
        }

        /* CTA Section */
        .cta-section {
            padding: 5rem 1rem;
            background: var(--gradient);
            color: var(--white);
            text-align: center;
        }

            .cta-section h2 {
                font-size: 2.5rem;
                font-weight: 700;
                margin-bottom: 1rem;
            }

            .cta-section p {
                font-size: 1.2rem;
                max-width: 700px;
                margin: 0 auto 2rem;
                opacity: 0.9;
            }

            .cta-section .btn {
                padding: 0.75rem 2.5rem;
                font-weight: 600;
                border-radius: 50px;
                transition: all 0.3s ease;
                box-shadow: var(--shadow);
            }

            .cta-section .btn-light {
                background-color: var(--white);
                color: var(--primary);
                border: 2px solid var(--white);
            }

                .cta-section .btn-light:hover {
                    background-color: transparent;
                    color: var(--white);
                    transform: translateY(-3px);
                }

        /* Footer */
        .footer {
            background-color: var(--dark);
            color: var(--white);
            padding: 3rem 1rem 1.5rem;
        }

            .footer h5 {
                font-size: 1.2rem;
                margin-bottom: 1.5rem;
                font-weight: 600;
            }

            .footer ul {
                list-style: none;
                padding: 0;
            }

                .footer ul li {
                    margin-bottom: 0.75rem;
                }

                    .footer ul li a {
                        color: rgba(255, 255, 255, 0.7);
                        text-decoration: none;
                        transition: all 0.3s ease;
                    }

                        .footer ul li a:hover {
                            color: var(--white);
                            padding-left: 5px;
                        }

            .footer p {
                color: rgba(255, 255, 255, 0.7);
            }

        .social-links {
            display: flex;
            gap: 1rem;
        }

            .social-links a {
                display: inline-flex;
                align-items: center;
                justify-content: center;
                width: 40px;
                height: 40px;
                background: rgba(255, 255, 255, 0.1);
                border-radius: 50%;
                color: var(--white);
                transition: all 0.3s ease;
            }

                .social-links a:hover {
                    background: var(--primary);
                    transform: translateY(-3px);
                }

        .copyright {
            text-align: center;
            padding-top: 2rem;
            margin-top: 2rem;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            color: rgba(255, 255, 255, 0.5);
            font-size: 0.9rem;
        }

        /* Responsive Styles */
        @@media (max-width: 768px) {
            .hero__title

        {
            font-size: 2.5rem;
        }

        .hero__subtitle {
            font-size: 1.1rem;
        }

        .section-title h2 {
            font-size: 2rem;
        }

        .cta-section h2 {
            font-size: 2rem;
        }

        .hero__cta .btn {
            display: block;
            width: 100%;
            margin-bottom: 1rem;
        }

            .hero__cta .btn:last-child {
                margin-bottom: 0;
            }

        }

       @@media (max-width: 576px) {
            .hero__title

        {
            font-size: 2rem;
        }

        .hero__subtitle {
            font-size: 1rem;
        }

        .features, .how-it-works, .cta-section {
            padding: 3rem 1rem;
        }

        }

        /* Animation */
        @@keyframes fadeInUp {
            from

        {
            opacity: 0;
            transform: translateY(30px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }

        }

        .fade-in-up {
            animation: fadeInUp 0.8s ease forwards;
        }

        .delay-1 {
            animation-delay: 0.2s;
        }

        .delay-2 {
            animation-delay: 0.4s;
        }

        .delay-3 {
            animation-delay: 0.6s;
        }
    </style>
</head>
<body>
    <!-- Hero Section -->
    <section class="hero">
        
        <div class="hero__overlay"></div>
        <div class="hero__content container">
            <div class="hero__eyebrow fade-in-up">WHERE HEALTHCARE MEETS INNOVATION</div>
            <h1 class="hero__title fade-in-up delay-1">Healthcare made simple. Care made personal.</h1>
            <p class="hero__subtitle fade-in-up delay-2">
                Experience seamless medical care with our integrated platform that connects patients, doctors,
                and healthcare providers in one secure digital space.
            </p>
            <div class="hero__cta fade-in-up delay-3">
                <a class="btn btn-light btn-lg me-2" asp-area="Identity" asp-page="/Account/Register">Get Started</a>
                <a class="btn btn-outline-light btn-lg" href="#features">Learn More</a>
            </div>
        </div>
    </section>

    <!-- Features Section -->
    <section class="features" id="features">
        <div class="container">
            <div class="section-title">
                <h2>Why Choose Medicare Connect</h2>
                <p>Our platform is designed to streamline healthcare management for both patients and providers with cutting-edge features.</p>
            </div>
            <div class="row g-4">
                <div class="col-md-4">
                    <div class="feature-card">
                        <div class="feature-icon">
                            <i class="fas fa-calendar-check"></i>
                        </div>
                        <h3>Easy Appointment Scheduling</h3>
                        <p>Book appointments with healthcare providers in just a few clicks. Receive reminders and manage your schedule effortlessly.</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="feature-card">
                        <div class="feature-icon">
                            <i class="fas fa-file-medical"></i>
                        </div>
                        <h3>Digital Health Records</h3>
                        <p>Access your medical history, test results, and prescriptions securely from anywhere, at any time.</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="feature-card">
                        <div class="feature-icon">
                            <i class="fas fa-video"></i>
                        </div>
                        <h3>Virtual Consultations</h3>
                        <p>Connect with healthcare professionals through secure video calls for remote diagnosis and treatment.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- How It Works Section -->
    <section class="how-it-works">
        <div class="container">
            <div class="section-title">
                <h2>How Medicare Connect Works</h2>
                <p>Getting started with our platform is simple and straightforward.</p>
            </div>
            <div class="row g-4">
                <div class="col-md-4">
                    <div class="step-card">
                        <div class="step-number">1</div>
                        <h3>Create Your Account</h3>
                        <p>Sign up in minutes with your basic information and verify your identity securely.</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="step-card">
                        <div class="step-number">2</div>
                        <h3>Set Up Your Profile</h3>
                        <p>Complete your medical profile and preferences to personalize your healthcare experience.</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="step-card">
                        <div class="step-number">3</div>
                        <h3>Start Connecting</h3>
                        <p>Book appointments, message providers, and manage your health journey with ease.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- CTA Section -->
    <section class="cta-section">
        <div class="container">
            <h2>Ready to Transform Your Healthcare Experience?</h2>
            <p>Join thousands of patients and providers who are already benefiting from Medicare Connect's innovative platform.</p>
            <a class="btn btn-light btn-lg" asp-area="Identity" asp-page="/Account/Register">Get Started Today</a>
        </div>
    </section>

    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <div class="row">
                <div class="col-lg-4 col-md-6 mb-4">
                    <h5>Medicare Connect</h5>
                    <p>Transforming healthcare through technology. Our mission is to make quality healthcare accessible to everyone.</p>
                    <div class="social-links mt-3">
                        <a href="#"><i class="fab fa-facebook-f"></i></a>
                        <a href="#"><i class="fab fa-twitter"></i></a>
                        <a href="#"><i class="fab fa-linkedin-in"></i></a>
                        <a href="#"><i class="fab fa-instagram"></i></a>
                    </div>
                </div>
                <div class="col-lg-2 col-md-6 mb-4">
                    <h5>Quick Links</h5>
                    <ul>
                        <li><a href="#">Home</a></li>
                        <li><a href="#features">Features</a></li>
                        <li><a href="#">About Us</a></li>
                        <li><a href="#">Contact</a></li>
                    </ul>
                </div>
                <div class="col-lg-3 col-md-6 mb-4">
                    <h5>Services</h5>
                    <ul>
                        <li><a href="#">Appointments</a></li>
                        <li><a href="#">Telemedicine</a></li>
                        <li><a href="#">Health Records</a></li>
                        <li><a href="#">Prescriptions</a></li>
                    </ul>
                </div>
                <div class="col-lg-3 col-md-6 mb-4">
                    <h5>Contact Us</h5>
                    <ul>
                        <li><i class="fas fa-map-marker-alt me-2"></i> 123 Healthcare Ave, Medical City</li>
                        <li><i class="fas fa-phone me-2"></i> (555) 123-4567</li>
                        <li><i class="fas fa-envelope me-2"></i> info@medicareconnect.com</li>
                    </ul>
                </div>
            </div>
            <div class="copyright">
                <p>&copy; 2023 Medicare Connect. All rights reserved.</p>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Simple animation on scroll
        document.addEventListener('DOMContentLoaded', function() {
            const fadeElements = document.querySelectorAll('.fade-in-up');

            const fadeInOnScroll = function() {
                fadeElements.forEach(element => {
                    const elementTop = element.getBoundingClientRect().top;
                    const elementVisible = 150;

                    if (elementTop < window.innerHeight - elementVisible) {
                        element.classList.add('fade-in-up');
                    }
                });
            };

            // Check on load
            fadeInOnScroll();

            // Check on scroll
            window.addEventListener('scroll', fadeInOnScroll);
        });
    </script>
</body>
</html>